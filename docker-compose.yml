version: '3.8'

services:
  frontend:
    build:
      context: .
    ports:
      - "3000:3000"
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    environment:
      - SMTP_USER=aborandemir@gmail.com
      - SMTP_PASS=cfdm sqvp lwhd ldov
